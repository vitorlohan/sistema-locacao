# ============================================================
# Sistema de Locação — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores
# Secrets são auto-gerados no primeiro uso se não configurados
# ============================================================

# Servidor
PORT=3000

# Banco de dados
DB_PATH=./data/locacao.db

# JWT (auto-gerados se não definidos)
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=30m
JWT_REFRESH_EXPIRES_IN=7d

# Criptografia (auto-gerado se não definido)
ENCRYPTION_KEY=

# Backup
BACKUP_DIR=./backups
BACKUP_INTERVAL_MS=14400000
MAX_BACKUPS=10

# Segurança
MAX_FAILED_ATTEMPTS=5
LOCKOUT_MINUTES=15

# Licenciamento
LICENSE_SERVER_URL=https://central-licencas.onrender.com
LICENSE_SECRET=
LICENSE_PATH=./.license
